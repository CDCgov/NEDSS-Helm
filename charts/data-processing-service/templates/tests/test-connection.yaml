apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-processing-service.fullname" . }}-test-connection"
  labels:
    {{- include "data-processing-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data-processing-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
