apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "liquibase.name" . }}
  labels:
    {{- include "liquibase.labels" . | nindent 4 }}
data:
{{/*  {{- range $path, $_ :=  .Files.Glob  "data/master/*" }}*/}}
{{/*    {{ $path | trimPrefix "data/master/" }}: |-*/}}
{{/*  {{ $.Files.Get $path | indent 4 }}*/}}
{{/*    {{ end }}*/}}
      {{- $path := printf "data/*" }}
      {{ .Files.Glob $path | indent 4  }}
