apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "liquibase.name" . }}
  labels:
    {{- include "liquibase.labels" . | nindent 4 }}
data:
  {{- range $path, $_ :=  .Files.Glob  "db/*" }}
    {{ $path | trimPrefix "db/" }}: |-
{{ $.Files.Get $path | indent 6 }}
    {{ end }}
