apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dataingestion-service.fullname" . }}
  labels:
    {{- include "dataingestion-service.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "dataingestion-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "dataingestion-service.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "dataingestion-service.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: DI_DBSERVER
              value: {{ .Values.jdbc.dbserver }}
            - name: DI_DBNAME
              value: {{ .Values.jdbc.dbname }}
            - name: DI_USERNAME
              value: {{ .Values.jdbc.username }}
            - name: DI_PASSWORD
              value: {{ .Values.jdbc.password }}
            - name: DI_NBS_DBSERVER
              value: {{ .Values.nbs.dbserver }}
            - name: DI_NBS_DBNAME
              value: {{ .Values.nbs.dbname }}
            - name: DI_NBS_DBUSER
              value: {{ .Values.nbs.username }}
            - name: DI_NBS_DBPASSWORD
              value: {{ .Values.nbs.password }}
            - name: BOOTSTRAP_SERVERS
              value: {{ .Values.kafka.cluster }}
            - name: DI_LOG_PATH
              value: {{ .Values.log.path }}
            - name: DI_RSA_PRIVATE_KEY
              value: {{ .Values.rsa.privateKey }}
            - name: DI_RSA_PUBLIC_KEY
              value: {{ .Values.rsa.publicKey }}
          volumeMounts:
          - mountPath: /usr/share/dataingestion/data
            name: di-persistent-storage     
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
      - name: di-persistent-storage
        persistentVolumeClaim:
          claimName: {{ include "dataingestion-service.fullname" . }}-di-pvc
