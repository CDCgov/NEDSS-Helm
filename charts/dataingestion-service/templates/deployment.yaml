apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dataingestion-service.fullname" . }}
  labels:
    {{- include "dataingestion-service.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "dataingestion-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "dataingestion-service.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "dataingestion-service.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          command: ['java', '-jar', 'api.jar']
          args: ['--spring.datasource.url={{ .Values.jdbc.url }}', '--spring.datasource.username={{ .Values.jdbc.username }}', '--spring.datasource.password={{ .Values.jdbc.password }}','--spring.datasource.nbs.url={{ .Values.jdbc.nbs.url }}', '--spring.datasource.nbs.username={{ .Values.jdbc.nbs.username }}', '--spring.datasource.nbs.password={{ .Values.jdbc.nbs.password }}', '--nbs.wildfly.url=https://{{ .Values.nbsExternalName }}']
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
