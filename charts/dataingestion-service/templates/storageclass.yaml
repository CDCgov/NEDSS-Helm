kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: {{ include "dataingestion-service.fullname" . }}-di-sc
provisioner: efs.csi.aws.com
parameters:
  provisioningMode: efs-ap
  fileSystemId: {{ .Values.pvc.diPvClaim.storageClass | quote }}
  directoryPerms: "700"