apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rti-cache-service.fullname" . }}-test-connection"
  labels:
    {{- include "rti-cache-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rti-cache-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
