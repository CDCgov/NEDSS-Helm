apiVersion: v1
kind: Service
metadata:
  name: {{ include "nbs-kafka.fullname" . }}
  labels:
    {{- include "nbs-kafka.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nbs-kafka.selectorLabels" . | nindent 4 }}