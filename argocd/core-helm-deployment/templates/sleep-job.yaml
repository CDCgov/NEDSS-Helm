apiVersion: batch/v1
kind: Job
metadata:
  name: nj-job1
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  template:
    spec:
      containers:
      - name: job
        image: busybox
        command: ["sh", "-c", "sleep 30 && echo Running job in wave 0"]
      restartPolicy: Never